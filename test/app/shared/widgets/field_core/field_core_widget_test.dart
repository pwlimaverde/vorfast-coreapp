import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:coreapp/app/shared/widgets/field_core/field_core_widget.dart';

void main() {
//  testWidgets('FieldCoreWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(FieldCoreWidget()));
//    final textFinder = find.text('FieldCore');
//    expect(textFinder, findsOneWidget);
//  });
}
