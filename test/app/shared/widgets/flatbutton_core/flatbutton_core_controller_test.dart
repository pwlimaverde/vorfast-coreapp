import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:coreapp/app/shared/widgets/flatbutton_core/flatbutton_core_controller.dart';
import 'package:coreapp/app/app_module.dart';

void main() {
  initModule(AppModule());
  // FlatbuttonCoreController flatbuttoncore;
  //
  setUp(() {
    //     flatbuttoncore = AppModule.to.get<FlatbuttonCoreController>();
  });

  group('FlatbuttonCoreController Test', () {
    //   test("First Test", () {
    //     expect(flatbuttoncore, isInstanceOf<FlatbuttonCoreController>());
    //   });

    //   test("Set Value", () {
    //     expect(flatbuttoncore.value, equals(0));
    //     flatbuttoncore.increment();
    //     expect(flatbuttoncore.value, equals(1));
    //   });
  });
}
