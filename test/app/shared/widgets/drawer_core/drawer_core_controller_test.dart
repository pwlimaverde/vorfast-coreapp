import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:coreapp/app/shared/widgets/drawer_core/drawer_core_controller.dart';
import 'package:coreapp/app/app_module.dart';

void main() {
  initModule(AppModule());
  // DrawerCoreController drawercore;
  //
  setUp(() {
    //     drawercore = AppModule.to.get<DrawerCoreController>();
  });

  group('DrawerCoreController Test', () {
    //   test("First Test", () {
    //     expect(drawercore, isInstanceOf<DrawerCoreController>());
    //   });

    //   test("Set Value", () {
    //     expect(drawercore.value, equals(0));
    //     drawercore.increment();
    //     expect(drawercore.value, equals(1));
    //   });
  });
}
