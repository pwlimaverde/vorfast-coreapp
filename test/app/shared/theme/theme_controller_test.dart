import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:coreapp/app/shared/theme/theme_controller.dart';
import 'package:coreapp/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ThemeController theme;
  //
  setUp(() {
    //     theme = AppModule.to.get<ThemeController>();
  });

  group('ThemeController Test', () {
    //   test("First Test", () {
    //     expect(theme, isInstanceOf<ThemeController>());
    //   });

    //   test("Set Value", () {
    //     expect(theme.value, equals(0));
    //     theme.increment();
    //     expect(theme.value, equals(1));
    //   });
  });
}
